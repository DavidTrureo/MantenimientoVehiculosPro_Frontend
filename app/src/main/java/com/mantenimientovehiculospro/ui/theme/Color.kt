package com.mantenimientovehiculospro.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta Principal (Tema Oscuro Recomendado)
val NavyBlue = Color(0xFF0D1B2A)      // Fondo principal: azul marino oscuro, elegante
val SlateGray = Color(0xFF1B263B)     // Superficies (tarjetas): gris azulado, ligeramente más claro
val PacificBlue = Color(0xFF415A77)   // Elementos secundarios o decorativos
val AmberAccent = Color(0xFFFFC107)   // Acento principal (botones, íconos activos): ámbar vibrante
val TextWhite = Color(0xFFE0E1DD)     // Texto principal: blanco roto, cómodo para la vista

// Alternativa (Tema Claro)
val OffWhite = Color(0xFFF7F9FC)       // Fondo: blanco roto, más suave que el blanco puro
val White = Color(0xFFFFFFFF)         // Superficies (tarjetas): blanco puro
val DeepBlue = Color(0xFF0D47A1)       // Acento principal: azul corporativo y profesional
val MediumGray = Color(0xFF757575)     // Texto secundario
val DarkGray = Color(0xFF212121)       // Texto principal

// Colores Semánticos (Consistentes en ambos temas)
val ErrorRed = Color(0xFFD32F2F)
val SuccessGreen = Color(0xFF4CAF50)
val WarningYellow = Color(0xFFFFA000)      // Coincide con el acento ámbar, se puede ajustar si es necesario
val InfoBlue = Color(0xFF0288D1)
val NeutralGray = Color(0xFF9E9E9E)